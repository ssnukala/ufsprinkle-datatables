<script type="text/javascript" id="datatablejs01_{{dtoptions.htmlid}}">
{% autoescape 'js' %}
    jQuery(document).ready(function () {
    var dtoptions = {{ dtoptions |json_encode(constant('JSON_PRETTY_PRINT'))|raw }} ;
     dtoptions['columns']=[
        {% for column in fields %}
            {% include "components/column.html.twig" %}
        {% endfor %} ];
/* Initializing datatable */
        var dtobject = createDatatableOnPage(dtoptions);
        });
{% endautoescape %}
</script>
